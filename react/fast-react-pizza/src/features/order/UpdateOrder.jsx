import { useFetcher } from "react-router-dom"
import Button from "../../ui/Button";

function UpdateOrder(order) {
    const fetcher = useFetche()r;
    return (
        <fetcher.Form>
            <Button>Make priority</Button>
        </fetcher.Form>
    )
}

export default UpdateOrder
